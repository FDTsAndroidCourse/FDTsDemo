<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="onl.fdt.android.fdtsdemo">

    <!--for ch5-->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CyclerView.ListActivity"></activity>

        <activity android:name=".CyclerView.CardView.CardActivity"></activity>

        <activity android:name=".ch3.Activity.Work1Activity"></activity>

        <activity android:name=".ch3.Activity.Work2Activity"></activity>

        <activity android:name=".ch3.Activity.Work3Activity"></activity>

        <activity android:name=".ch4.ClockApplication"></activity>

        <activity android:name=".ch5.Ch5Activity"></activity>

        <activity android:name=".ch6.activity.TodoListActivity"></activity>

        <activity android:name=".ch6.activity.AddNoteActivity"></activity>
    </application>

</manifest>